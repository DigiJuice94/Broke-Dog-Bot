import { config } from "./config.ts"; import type { EntryLane } from "./types.ts";
export function choosePositionUsd(input:{score:number;confidence:number;spendableUsd:number;routeQuality:number;multiTrend:boolean;lane:EntryLane;openPositions:number}){const {score,confidence,spendableUsd,routeQuality,multiTrend,lane,openPositions}=input;if(spendableUsd<config.minPositionUsd)return 0;const scoreFactor=Math.max(.35,Math.min(1,(score-75)/25)),confidenceFactor=Math.max(.6,Math.min(1,confidence/100)),routeFactor=Math.max(.65,Math.min(1,routeQuality/100)),trendFactor=multiTrend?1:.92;let capPct=config.maxPositionWalletPct;if(openPositions>=2)capPct=Math.min(capPct,config.thirdPositionWalletPct);if(lane==="NORMAL")capPct=Math.min(capPct,22);if(lane==="FLAME")capPct=Math.min(capPct,25);const target=spendableUsd*(capPct/100)*scoreFactor*confidenceFactor*routeFactor*trendFactor;return Math.min(spendableUsd,Math.max(config.minPositionUsd,target))}
